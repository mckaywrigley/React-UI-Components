import React from 'react';
import './Header.css';

const HeaderContent = () => {
    return (
        <div className="header-content">
            <p>This is an example of a component that is supposed to look like the body of a Tweet. This probably is going way over 140 characers. I don't care. In this app there is no character limit.</p>
        </div>
    );
}

export default HeaderContent;